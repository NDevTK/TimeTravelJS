{
  "name": "timetraveljs",
  "version": "0.1.0",
  "private": true,
  "description": "Time-travel debugging for JavaScript in the browser: QuickJS on WebAssembly with copy-on-write memory snapshots. The site itself is buildless — dependencies are vendored under vendor/.",
  "type": "module",
  "scripts": {
    "start": "node tools/serve.mjs",
    "test": "node --test \"tests/*.test.mjs\"",
    "test:e2e": "node tests/e2e.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.49.0"
  }
}
